---
name: cli-setup
description: Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.

## Quick install

```bash
npm install -g dragonrouter   # or: npx dragonrouter
dragonrouter --version
```

## Subcommands

### `autostart`

**Example:**

```bash
dragonrouter autostart
```

### `autostart enable`

**Example:**

```bash
dragonrouter autostart enable
```

### `autostart disable`

**Example:**

```bash
dragonrouter autostart disable
```

### `autostart toggle`

**Example:**

```bash
dragonrouter autostart toggle
```

### `autostart status`

**Example:**

```bash
dragonrouter autostart status
```

### `config`

Show or update CLI tool configuration

**Example:**

```bash
dragonrouter config
```

### `config list`

List all CLI tools and config status

**Flags:**

- `--json`

**Example:**

```bash
dragonrouter config list
```

### `config get <tool>`

Show current config for a tool

**Flags:**

- `--json`

**Example:**

```bash
dragonrouter config get <tool>
```

### `config set <tool>`

Write config for a tool

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
dragonrouter config set <tool>
```

### `config validate <tool>`

Validate config format without writing

**Flags:**

- `--model <model>`
- `--json`

**Example:**

```bash
dragonrouter config validate <tool>
```

### `config opencode`

Generate OpenCode config (alias for 

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
dragonrouter config opencode
```

### `config lang`

**Example:**

```bash
dragonrouter config lang
```

### `config get`

**Flags:**

- `--json`

**Example:**

```bash
dragonrouter config get
```

### `config set <code>`

**Flags:**

- `--force`

**Example:**

```bash
dragonrouter config set <code>
```

### `config list`

**Flags:**

- `--json`

**Example:**

```bash
dragonrouter config list
```

### `env`

Show and manage environment variables

**Example:**

```bash
dragonrouter env
```

### `env show`

Show current environment variables

**Flags:**

- `--json`

**Example:**

```bash
dragonrouter env show
```

### `env get <key>`

Get a single environment variable

**Example:**

```bash
dragonrouter env get <key>
```

### `env set <key> <value>`

Set an environment variable (current session only)

**Example:**

```bash
dragonrouter env set <key> <value>
```

### `setup`

**Flags:**

- `--password <value>`
- `--add-provider`
- `--provider <id>`
- `--provider-name <name>`
- `--api-key <value>`
- `--default-model <model>`
- `--provider-base-url <url>`
- `--test-provider`
- `--non-interactive`
- `--list`

**Example:**

```bash
dragonrouter setup
```

### `update`

**Flags:**

- `--check`
- `--apply`
- `--changelog`
- `--dry-run`
- `--no-backup`
- `--yes`

**Example:**

```bash
dragonrouter update
```
